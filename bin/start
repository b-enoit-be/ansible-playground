#!/usr/bin/env bash
set -euxo pipefail

COMPOSE_IGNORE_ORPHANS=True \
docker compose \
  --file "$(dirname $0)"/../docker/docker-compose.yml \
  run \
  --rm \
  --entrypoint ash \
  ansible_controller \
