plugin: community.docker.docker_containers
docker_host: unix://var/run/docker.sock
connection_type: ssh

compose:
  ansible_ssh_port: ansible_ssh_port | default(22, true)

keyed_groups:
  - key: docker_config.Labels.ansible_type
